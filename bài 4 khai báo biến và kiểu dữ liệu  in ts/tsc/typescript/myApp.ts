// ctrl+c để ẩn cmd đi
// tạo một file tsconfigured để khi mình tạo nhiều file thì nó chỉ chạy một lần mh ko phải tạo  nhiều làm gì
// dùng lệnh tsc --init để tạo file tsconfig
// ctrl+shift + b: để chạy build or watch
var _mark = 9.5;
var info = 'toi ten la nam'
console.log(info);
console.log(_mark);
var demo = document.getElementById('demo') as HTMLElement;
demo.innerHTML = info;